import { ProfanityFilterDirective } from './profanity-filter.directive';

describe('ProfanityFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfanityFilterDirective();
    expect(directive).toBeTruthy();
  });
});
